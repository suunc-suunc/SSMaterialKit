import XCTest
@testable import SSMaterialKit

final class SSMaterialKitTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SSMaterialKit().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
